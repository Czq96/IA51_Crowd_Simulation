package ui

interface EnvironmentListener extends EventListener {

			/** Invoked when the environment has finished to apply all changed.
		 * 
		 * @param event
		 */
	def environmentChanged(^event : EnvironmentEvent)

}
